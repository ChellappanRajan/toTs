# CLI to convert JSON to Typescript Interface

## Installation

```sh
# Using NPM:
npm install -g toTs

```

## Usage


## CLI

A CLI utility is provided with this package.

```sh
toTs --file="<file.json>"
# or
toTs -f="<file.json>"
